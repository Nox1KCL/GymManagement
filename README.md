# GymManagement System (Data Engineering Edition)

Система управління даними користувачів спортивного залу, побудована на **Python 3.13**. Проєкт демонструє підхід до створення відмовостійких систем збереження даних без використання SQL-двигунів, фокусуючись на **прямій маніпуляції структурованими даними (JSON)**.

Це рішення емулює роботу з документо-орієнтованими базами даних та реалізує механізми безпеки на рівні застосунку.

### 🛠 Технічні особливості (Data Engineering Focus):
* **Custom Data Persistence:** Реалізовано власний рушій (ETL-lite) для збереження та відновлення стану системи через JSON (`utils.py`, `gymsystem.py`).
* **Memory Management:** Використання **Python Generators** (`yield` у методах `browse_users`, `delegator`) для ледачої (lazy) обробки потоку даних користувачів.
* **Security & Integrity:** Хешування паролів (SHA-256) та валідація вхідних даних на відповідність типам.
* **Logging System:** Вбудована система логування подій (`app.log`) для аудиту дій користувачів.

### 📋 Функціонал:
* **Role-Based Access Control (RBAC):** Розділення прав доступу (Member/Admin) через наслідування класів (`User` -> `Member`, `Admin`).
* **CRUD Operations:** Повний цикл управління сутностями в "сирому" форматі даних.
* **Analytics Ready:** Структура даних спроектована так, щоб бути готовою для експорту в аналітичні системи (наприклад, для прогнозування відвідуваності).

### 🚀 Stack:
* **Core:** Python 3.13 (Type Hinting, OOP, Generators)
* **Data:** JSON (Nested structures)
* **Security:** `hashlib`, `uuid`
* **Utils:** `logging`, `pathlib`

### Project Structure:
* `app/classes/` - Бізнес-логіка та моделі даних.
* `app/userdata/` - Сховище даних (JSON).
* `app/utils.py` - Утиліти для обробки та валідації даних.
